import styled from 'styled-components';

const StyledRow = styled.div`
	position: relative;
	margin-left: auto;
	margin-right: auto;
	padding-left: 1em;
	padding-right: 1em;
	max-width: 1200px;
`;

export {
    StyledRow
};